[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "fast_glcm"
version = "0.1.0"
description = "Rust extension for fast 2D/3D GLCM counts"
requires-python = ">=3.9"